name: Deploy UI to CloudRun

on:
  workflow_call:
    inputs:
      gcp_region:
        required: true
        type: string
      gcp_project_id:
        required: true
        type: string
      image_repo:
        required: true
        type: string    
      image_tag:
        required: true
        type: string
      service_name:
        required: true
        type: string
      port:
        required: true
        type: string
      cpu:  
        required: false
        type: string
        default: '1'
      memory:
        required: false
        type: string
        default: '512Mi'
      additionalflag:
        required: false
        type: string        

    secrets:
      gcp_workload_identity_provider:
        required: true
      gcp_service_account:
        required: true

jobs:
  deploy:
    permissions:
      contents: 'read'
      id-token: 'write'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - id: 'auth'
        name: 'Authenticate to Google Cloud'
        uses: 'google-github-actions/auth@v1'
        with:
          workload_identity_provider: ${{ secrets.gcp_workload_identity_provider }}
          service_account: ${{ secrets.gcp_service_account }}     

      - name: Deploy to Cloud Run
        run: |
          IMAGE_NAME="asia-southeast1-docker.pkg.dev/${{ inputs.gcp_project_id }}/${{ inputs.image_repo }}:${{ inputs.image_tag }}"         

          if [[ -n "${{ inputs.cpu }}" ]]; then
            CPU_VALUE="${{ inputs.cpu }}"
          else
            CPU_VALUE="1"
          fi

          if [[ -n "${{ inputs.memory }}" ]]; then
            MEMORY_VALUE="${{ inputs.memory }}"
          else
            MEMORY_VALUE="512Mi"
          fi

          # Build optional additional flag
          ADDITIONAL_FLAG=""
          if [[ -n "${{ inputs.additionalflag }}" ]]; then
            ADDITIONAL_FLAG="--${{ inputs.additionalflag }}"
          fi

          gcloud run deploy ${{ inputs.service_name }} \
            --platform managed \
            --execution-environment=gen2 \
            --region ${{ inputs.gcp_region }} \
            --project ${{ inputs.gcp_project_id }} \
            --allow-unauthenticated \
            --image $IMAGE_NAME \
            --memory $MEMORY_VALUE \
            --cpu $CPU_VALUE \
            --concurrency 80 \
            --timeout 300s \
            --port ${{ inputs.port }} \
            $ADDITIONAL_FLAG